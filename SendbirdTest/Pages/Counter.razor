@page "/counter"
@using Microsoft.Extensions.Logging;
@inject ILogger<Counter> logger;

<h1>Counter</h1>

<p>Current count: @currentCount</p>

<button class="btn btn-primary" @onclick="IncrementCount">Click me</button>

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        logger.LogWarning("Someone has clicked me!");
        currentCount++;
    }

    public SendbirdTest.SendBirdChat.Services.SendbirdService clientChatService { get; set; }

    protected override void OnInitialized()
    {
        clientChatService = new SendBirdChat.Services.SendbirdService("12AB2505-9C52-4DD9-8644-C3612AD3BB54", "UID1");

        logger.LogWarning("after init Sendbird");
        /*logger.LogWarning("Status:" + clientChatService.GetConnectionStatus());*/
    }
}
